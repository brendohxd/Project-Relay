---
description: Read Project Relay documentation, run verification diagnostics, and summarize the implementation state.
---

Read @README.md, @project/status.json, @docs/ARCHITECTURE.md,
@docs/PROTOCOL.md, @docs/PUBLICATION_BOUNDARY.md, @CONTRIBUTING.md,
@SECURITY.md, and @docs/ANTIGRAVITY_HANDOFF.md.

Run git status --short --branch, git log -1 --oneline, node --version,
npm --version, npm run doctor, and npm run check.

Do not edit anything yet. Summarise the real implementation state, current
milestone, pre-existing changes that must be preserved, and the smallest safe
work packet.
